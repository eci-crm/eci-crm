import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";
import { ThemeProvider } from "next-themes";
import { Toaster } from "@/components/ui/toaster";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "ECI CRM - Enterprise Customer Intelligence",
  description: "Modern CRM system for proposal management, client tracking, and reporting. Built by Irfan Munir.",
  keywords: ["CRM", "Proposal Management", "Client Management", "ECI", "Enterprise"],
  authors: [{ name: "Irfan Munir" }],
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={inter.className}>
        <ThemeProvider attribute="class" defaultTheme="system" enableSystem>
          {children}
          <Toaster />
        </ThemeProvider>
      </body>
    </html>
  );
}
